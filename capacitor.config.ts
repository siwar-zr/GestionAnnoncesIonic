import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicannonces',
  appName: 'Annonces',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
